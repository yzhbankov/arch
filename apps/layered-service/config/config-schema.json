{
  "serverPort": ["required", "positive_integer"],
  "db": ["required", {
    "nested_object": {
      "host" : ["required", "string"],
      "port" : ["required", "positive_integer"],
      "prefix": ["required", "string"]
    }
  }],
  "zmq": ["required", {
    "nested_object": {
      "pushAddress": ["required", "string"],
      "pullAddress": ["required", "string"],
      "log": ["required", "boolean"]
    }
  }]
}
